# Build Helpers

Convenience scripts for building this MsQuic fork and related perf binaries.
The main source tree remains rooted at the repository top level; these scripts
are wrappers, not part of upstream MsQuic's standard `scripts/` directory.
